<main class="other tab flexcol {{tab.cssClass}}" data-tab="other" data-group="primary">
	{{#each otherCategories}}
		{{#if table}}
			<section class="desc resource-content flexgrow-1">
				<header class="items-main-header">
				<span class="items-main">
					{{localize label}}
				</span>
				</header>
				{{{table}}}
			</section>
		{{/if}}
	{{/each}}
</main>