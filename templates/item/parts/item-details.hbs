<section class="tab details flexcol fu-scrollable {{tab.cssClass}}" data-group="primary" data-tab="details">

	{{#if featureTemplate}}
		{{> (lookup this 'featureTemplate') }}
	{{/if}}
	{{#if optionalTemplate}}
		{{> (lookup this 'optionalTemplate') }}
	{{/if}}

</section>