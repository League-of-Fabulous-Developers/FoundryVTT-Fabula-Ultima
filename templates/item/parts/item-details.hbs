<section class="tab details flexcol {{tab.cssClass}}" data-group="primary" data-tab="details">

	{{#if featureTemplate}}
		{{> (lookup this 'featureTemplate') }}
	{{/if}}
	{{#if optionalTemplate}}
		{{> (lookup this 'optionalTemplate') }}
	{{/if}}

</section>