{{ log "in single choice" this }}
<fieldset>
	<label>
		{{localize templateData.label}}
		<select name="{{fieldPrefix}}.{{templateData.name}}">
			{{selectOptions templateData.choices selected=(lookup effect templateData.name) localize=true}}
		</select>
	</label>
</fieldset>