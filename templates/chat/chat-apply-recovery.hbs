<section>
    {{{localize message actor=actor amount=amount resource=resourceLabel from=from type=type}}}
</section>

<section style="margin-top: 4px; display: flex; justify-content: space-between; ">
    <div>
        <a data-action="revertResourceGain" data-uuid="{{uuid}}" data-resource="{{resource}}" data-amount="{{amount}}" data-key="{{key}}" data-tooltip="{{localize 'FU.ChatRevertResourceGain'}}">
            <span class='fu-tag button flex-group-center' style="font-size: 1rem;"><i class="icon fas fa-undo"></i></span>
        </a>
    </div>
</section>