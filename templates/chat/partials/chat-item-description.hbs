<div>
    <details {{#unless collapseDescriptions}}open{{/unless}}>
        <summary style="margin: 10px 0">{{localize 'FU.Description'}}</summary>
        <div class='chat-desc'>
            {{#if summary}}
            <blockquote class='summary quote'>{{summary}}</blockquote>
            {{/if}}
            {{{description}}}
        </div>
    </details>
</div>