<div class="toggle-section tags {{#if (getGameSetting 'optionChatMessageHideTags')}}hidden{{/if}}">
    <div class="fu-tags">
        <div class="fu-tag">
            {{#if spell.duration}}
                {{spell.duration}}
            {{else}}
                {{localize 'FU.Duration'}}
            {{/if}}
        </div>
        <div class="fu-tag">
            {{spell.target}}
        </div>
        <div class="fu-tag">
            {{#if spell.mpCost}}
                {{#if (eq spell.target 'multiple') }}
                    {{spell.mpCost}} {{localize 'FU.MindAbbr'}} Per Target
                {{else}}
                    {{spell.mpCost}} {{localize 'FU.MindAbbr'}}
                {{/if}}
            {{else}}
                {{localize 'FU.MindPointCost'}}
            {{/if}}
        </div>
    </div>
</div>

{{#if spell.opportunity}}
    <div class="toggle-section quality {{#if (getGameSetting 'optionChatMessageHideQuality')}}hidden{{/if}}">
        {{> 'systems/projectfu/templates/chat/partials/chat-item-opportunity.hbs' opportunity=spell.opportunity}}
    </div>
{{/if}}

{{#if (or spell.summary spell.description)}}
    <div class="toggle-section description {{#if (getGameSetting 'optionChatMessageHideDescription')}}hidden{{/if}}">
        {{> 'systems/projectfu/templates/chat/partials/chat-item-description.hbs' summary=spell.summary description=spell.description}}
    </div>
{{/if}}