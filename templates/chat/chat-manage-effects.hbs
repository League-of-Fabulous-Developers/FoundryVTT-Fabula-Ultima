<section>
        <div class='projectfu'>
            <table>
            <caption>{{localize event}}, {{localize 'FU.Round'}} {{round}}</caption>
                <thead>
                <tr>
                    <th>{{localize 'FU.Effect'}}</th>
                    <th>{{localize 'FU.Remaining'}}</th>
                </tr>
                </thead>
                <tbody>
                {{#each effects as |effect|}}
                    <tr>
                        <td>
                            <a data-action="display" data-description="{{effect.description}}" data-actor-id="{{../uuid}}">
                                <img class="image-icon-s" src="{{ effect.img }}" data-tooltip="{{ effect.name }}" />
                                {{effect.name}}
                            </a>
                        </td>

                        <td>
                            {{#if (eq effect.remaining 0)}}
                                <span>Expired</span>
                                <a data-action="removeEffect" data-name="{{effect.name}}" data-id="{{effect.id}}" data-actor-id="{{../uuid}}">
                                    <span class='fu-tag button flex-group-center' style="font-size: 1rem;"><i class="icon fas fa-trash }"></i></span>
                                </a>
                            {{else}}
                                <span>{{effect.remaining}}</span>
                            {{/if}}
                        </td>
                    </tr>
                {{/each}}
                </tbody>
            </table>
        </div>
</section>