<label class="fu-rule-element__property">
	<span>{{localize 'FU.Status'}}</span>
	<select name="{{pfuConcat path ".status"}}">
		{{selectOptions options.statusEffects selected=trigger.status sort=true localize=true}}
	</select>
</label>

<label class="fu-rule-element__property">
	<span>{{localize 'FU.Change'}}</span>
	<select name="{{pfuConcat path ".change"}}">
		{{selectOptions options.collectionChange selected=trigger.change sort=true localize=true}}
	</select>
</label>