<label class="fu-rule-element__property">
	<span>{{localize 'FU.Id'}}</span>
	<input type="text" name="{{pfuConcat path ".identifier"}}"
		   placeholder="{{localize 'FU.All'}}"
		   value="{{ action.identifier }}" />
</label>