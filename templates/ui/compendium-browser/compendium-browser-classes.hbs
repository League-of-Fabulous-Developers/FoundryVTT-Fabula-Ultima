<section class="tab classes {{tab.cssClass}} background"
		 data-tab="classes" data-group="primary">
	<main class="fu-application__content">
		{{#each tables as |table|}}
			{{{table}}}
		{{/each}}
	</main>
</section>